# Ansible

Use Ansible for operational automation, not as the main infrastructure tool.

## Good Uses Here

- configure a small utility EC2 instance
- install Docker, kubectl, helm, aws cli
- run maintenance or backup scripts
- keep host setup reproducible

## Note

If no EC2 exists yet, you do not need to run Ansible immediately.
